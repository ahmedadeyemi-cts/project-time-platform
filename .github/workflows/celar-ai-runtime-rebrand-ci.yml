name: Validate Celar AI Runtime Rebrand and Demo Chat

on:
  push:
    branches:
      - 'feature/module-011-celar-ai-runtime-rebrand-*'
  pull_request:
    paths:
      - '.github/workflows/celar-ai-runtime-rebrand-ci.yml'
      - '.github/workflows/celar-ai-enterprise-platform-ci.yml'
      - '.github/workflows/group5-financial-operations-recovery-ci.yml'
      - '.github/workflows/group7-ai-help-system-guide-ci.yml'
      - '.github/workflows/module064-automatic-provider-health-ci.yml'
      - '.github/workflows/pulse-ai-system-intelligence-ci.yml'
      - '.github/workflows/pulse-ai-help-chat-usability-ci.yml'
      - 'database/migrations/061_celar_ai_capability_routing.sql'
      - 'database/rollback/061_celar_ai_capability_routing_rollback.sql'
      - 'tests/test-pulse-ai-system-intelligence-migration-054.sh'
      - 'docs/modules/module-011-pulse-ai/CELAR-AI-RUNTIME-REBRAND.md'
      - 'src/backend/ProjectTime.Api/Ai/CelarAiBrandProfile.cs'
      - 'src/backend/ProjectTime.Api/Ai/PulseAiProductKnowledgeCatalog.cs'
      - 'src/backend/ProjectTime.Api/Ai/ProjectPulseAiServiceCollectionExtensions.cs'
      - 'src/backend/ProjectTime.Api/Modules/CelarAiBrandModule.cs'
      - 'src/backend/ProjectTime.Api/ProjectTime.Api.csproj'
      - 'src/frontend/project-time-web/package.json'
      - 'src/frontend/project-time-web/package-lock.json'
      - 'src/frontend/project-time-web/scripts/inject-celar-ai-runtime-rebrand.mjs'
      - 'src/frontend/project-time-web/scripts/validate-celar-ai-runtime-rebrand.mjs'
      - 'src/frontend/project-time-web/scripts/validate-module-011-pulse-ai.mjs'
      - 'src/frontend/project-time-web/scripts/validate-group-1-navigation-work-consolidation.mjs'
      - 'src/frontend/project-time-web/scripts/validate-module-011-private-document-pipeline.mjs'
      - 'src/frontend/project-time-web/scripts/validate-module-011-pulse-ai-deep-intelligence.mjs'
      - 'src/frontend/project-time-web/scripts/validate-module-011-system-intelligence-package.mjs'
      - 'src/frontend/project-time-web/scripts/validate-pulse-ai-help-chat-usability.mjs'
      - 'src/frontend/project-time-web/src/CelarAiProviderBridgePanel.jsx'
      - 'src/frontend/project-time-web/src/celar-ai-provider-bridge-panel.css'
      - 'src/frontend/project-time-web/src/module-availability-registry.js'

permissions:
  contents: read

concurrency:
  group: celar-ai-runtime-rebrand-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 100

    steps:
      - name: Check out exact source
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Verify owned source scope or run regression mode
        shell: bash
        run: |
          set -Eeuo pipefail
          BASE_BRANCH="${GITHUB_BASE_REF:-main}"
          HEAD_BRANCH="${GITHUB_HEAD_REF:-${GITHUB_REF_NAME}}"
          git fetch origin "$BASE_BRANCH" --no-tags
          BASE_REF="$(git merge-base "origin/$BASE_BRANCH" HEAD)"
          test -n "$BASE_REF"
          CHANGED="$(git diff --name-only "$BASE_REF"...HEAD)"
          printf '%s\n' "$CHANGED"

          if [[ -n "$CHANGED" ]] && [[ -z "$(grep -Ev '^src/frontend/project-time-web/package(-lock)?\.json$' <<<"$CHANGED" || true)" ]]; then
            echo 'PROJECTPULSE_DEPENDENCY_ONLY=true' >> "$GITHUB_ENV"
            echo 'CELAR_AI_RUNTIME_REBRAND_VALIDATION_MODE=DEPENDENCY_ONLY' >> "$GITHUB_ENV"
            echo 'CELAR_AI_DEPENDENCY_ONLY_SCOPE=PASSED'
          elif [[ "$HEAD_BRANCH" == fix/remaining-release-train-specialized-workflow-convergence-* ]]; then
            WORKFLOW_ALLOWED='^\.github/workflows/(celar-ai-runtime-rebrand-ci|group5-financial-operations-recovery-ci|group7-ai-help-system-guide-ci|module064-automatic-provider-health-ci|pulse-ai-help-chat-usability-ci|pulse-ai-system-intelligence-ci)\.yml$'
            UNEXPECTED="$(grep -Ev "$WORKFLOW_ALLOWED" <<<"$CHANGED" || true)"
            if [[ -n "$UNEXPECTED" ]]; then
              echo 'The specialized workflow-convergence branch changed source outside its reviewed workflow boundary:' >&2
              printf '%s\n' "$UNEXPECTED" >&2
              exit 1
            fi
            echo 'CELAR_AI_RUNTIME_REBRAND_VALIDATION_MODE=WORKFLOW_CONVERGENCE' >> "$GITHUB_ENV"
            echo 'CELAR_AI_WORKFLOW_CONVERGENCE_SCOPE=PASSED'
          elif [[ "$HEAD_BRANCH" == feature/module-011-celar-ai-runtime-rebrand-* ]]; then
            test "$BASE_REF" = "$(git rev-parse "origin/$BASE_BRANCH")"
            ALLOWED='^(\.github/workflows/(celar-ai-runtime-rebrand-ci|deep-intelligence-read-contract-ci|pulse-ai-system-intelligence-ci|pulse-ai-help-chat-usability-ci)\.yml|docs/modules/module-011-pulse-ai/(CELAR-AI-RUNTIME-REBRAND|SYSTEM-INTELLIGENCE-AND-TROUBLESHOOTING)\.md|src/backend/ProjectTime\.Api/Ai/(CelarAiBrandProfile|PulseAiProductKnowledgeCatalog|ProjectPulseAiServiceCollectionExtensions|PulseAiSystemIntelligenceService|PulseAiSystemToolExecutor)\.cs|src/backend/ProjectTime\.Api/Modules/CelarAiBrandModule\.cs|src/backend/ProjectTime\.Api/ProjectTime\.Api\.csproj|src/frontend/project-time-web/package(-lock)?\.json|src/frontend/project-time-web/scripts/(inject-celar-ai-runtime-rebrand|inject-pulse-ai-system-chat-group7-compatibility|validate-celar-ai-runtime-rebrand|validate-module-011-pulse-ai|validate-group-1-navigation-work-consolidation|validate-module-011-private-document-pipeline|validate-module-011-pulse-ai-deep-intelligence|validate-module-011-system-intelligence-package|validate-pulse-ai-help-chat-usability)\.mjs|src/frontend/project-time-web/src/(CelarAiProviderBridgePanel\.jsx|celar-ai-provider-bridge-panel\.css|module-availability-registry\.js|HelpAssistant\.jsx|PulseAiSystemIntelligenceWorkbench\.jsx|WorkTaskBuilderPanel\.jsx|pulse-ai-system-chat\.css|pulse-ai-system-intelligence-workbench\.css)|deployment/containers/web/Dockerfile)$'
            UNEXPECTED="$(grep -Ev "$ALLOWED" <<<"$CHANGED" || true)"
            if [[ -n "$UNEXPECTED" ]]; then
              echo 'Unexpected Celar AI runtime-rebrand source scope:' >&2
              printf '%s\n' "$UNEXPECTED" >&2
              exit 1
            fi
            if grep -E '^(database/|scripts/.*deploy|\.github/workflows/projectpulse-deploy-|src/backend/ProjectTime\.Api/Ai/(ProjectPulseAiConfiguration|ProjectPulseAiRemoteProviders|ProjectPulseAiSecretStore)\.cs|src/backend/ProjectTime\.Api/Modules/AiProviderConfigurationModule\.cs)' <<<"$CHANGED"; then
              echo 'The runtime-rebrand feature branch overlaps a prohibited database, deployment, or provider-secret surface.' >&2
              exit 1
            fi
            echo 'CELAR_AI_RUNTIME_REBRAND_VALIDATION_MODE=FEATURE_SOURCE' >> "$GITHUB_ENV"
          else
            echo 'CELAR_AI_RUNTIME_REBRAND_VALIDATION_MODE=REGRESSION' >> "$GITHUB_ENV"
            echo "CELAR_AI_RUNTIME_REBRAND_REGRESSION_BRANCH=$HEAD_BRANCH"
          fi

          for protected in \
            '.github/workflows/projectpulse-deploy-runtime-direct-timer-recovery-test.yml' \
            '.github/workflows/validate-runtime-direct-timer-recovery-deployment.yml' \
            'scripts/validate-runtime-direct-timer-recovery-test-deployment.sh'; do
            if grep -Fxq "$protected" <<<"$CHANGED"; then
              echo "Protected deployment file changed: $protected" >&2
              exit 1
            fi
          done

          git diff --check "$BASE_REF"...HEAD
          echo "CELAR_AI_RUNTIME_REBRAND_BASE=$BASE_REF"
          echo 'CELAR_AI_RUNTIME_REBRAND_SOURCE_ISOLATION=PASSED'

      - name: Set up .NET
        if: env.PROJECTPULSE_DEPENDENCY_ONLY != 'true'
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4
        with:
          dotnet-version: '10.0.x'

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: src/frontend/project-time-web/package-lock.json

      - name: Validate source and preserved contracts
        working-directory: src/frontend/project-time-web
        shell: bash
        run: |
          set -Eeuo pipefail
          node --check ./scripts/inject-celar-ai-runtime-rebrand.mjs
          node --check ./scripts/validate-celar-ai-runtime-rebrand.mjs
          node ./scripts/validate-celar-ai-runtime-rebrand.mjs
          node ./scripts/validate-module-011-pulse-ai.mjs
          node ./scripts/validate-group-1-navigation-work-consolidation.mjs
          node ./scripts/validate-pulse-ai-help-chat-usability.mjs
          node ./scripts/validate-module-011-system-intelligence-package.mjs
          node ./scripts/validate-module-011-pulse-ai-deep-intelligence.mjs
          node ./scripts/validate-module-011-private-document-pipeline.mjs
          echo 'CELAR_AI_PRESERVED_PREBUILD_CONTRACTS=PASSED'

      - name: Verify durable conversation migration 054
        if: env.PROJECTPULSE_DEPENDENCY_ONLY != 'true'
        shell: bash
        run: |
          set -Eeuo pipefail
          chmod +x tests/test-pulse-ai-system-intelligence-migration-054.sh
          tests/test-pulse-ai-system-intelligence-migration-054.sh

      - name: Build ProjectTime API and verify endpoint registration
        if: env.PROJECTPULSE_DEPENDENCY_ONLY != 'true'
        shell: bash
        run: |
          set -Eeuo pipefail
          dotnet build src/backend/ProjectTime.Api/ProjectTime.Api.csproj --configuration Release | tee celar-ai-api-build.log
          grep -Fq '0 Error(s)' celar-ai-api-build.log
          GENERATED="$(find src/backend/ProjectTime.Api -maxdepth 8 -name Program.ScopedRbac.g.cs -print -quit)"
          test -n "$GENERATED"
          grep -Fq 'app.MapPulseAiSystemIntelligenceEndpoints();' "$GENERATED"
          grep -Fq 'app.MapCelarAiBrandEndpoints();' "$GENERATED"
          echo 'CELAR_AI_API_BUILD=PASSED'

      - name: Install frontend dependencies
        working-directory: src/frontend/project-time-web
        run: npm ci --no-audit --no-fund

      - name: Build complete frontend production bundle
        working-directory: src/frontend/project-time-web
        shell: bash
        run: |
          set -Eeuo pipefail
          npm run build | tee celar-ai-frontend-build.log
          test -s dist/index.html
          cat dist/assets/*.js > /tmp/celar-ai-runtime.js
          cat dist/assets/*.css > /tmp/celar-ai-runtime.css
          for marker in \
            'Ask Celar AI' \
            'Celar AI Workbench' \
            'Speed of light. Speed of delivery.' \
            '/api/celar-ai/v1/chat' \
            'Private intelligence and governed provider routing' \
            'Raw internal documents never use public providers' \
            'Enter sends · Shift+Enter adds a line' \
            'completed responses remain in conversation history'; do
            grep -Fq "$marker" /tmp/celar-ai-runtime.js
          done
          for marker in \
            '.celar-ai-provider-bridge' \
            '.celar-ai-provider-bridge__route-grid' \
            '.help-panel.pulse-ai-system-chat .help-messages' \
            'overflow-y:scroll' \
            'scrollbar-gutter:stable both-edges'; do
            grep -Fq "$marker" /tmp/celar-ai-runtime.css
          done
          echo 'CELAR_AI_FRONTEND_BUILD=PASSED'

      - name: Validate production web container build context
        shell: bash
        run: |
          set -Eeuo pipefail
          docker build \
            --file deployment/containers/web/Dockerfile \
            --tag "projectpulse-celar-ai:${GITHUB_SHA}" \
            .
          echo 'CELAR_AI_WEB_CONTAINER_BUILD=PASSED'

      - name: Publish validation summary
        shell: bash
        run: |
          set -Eeuo pipefail
          {
            echo '## Module 011 — Celar AI Runtime Rebrand and Demo Chat'
            echo
            echo "- Validation mode: ${CELAR_AI_RUNTIME_REBRAND_VALIDATION_MODE:-UNKNOWN}"
            echo '- Canonical identity, durable chat, keyboard, scroll, history, and provider bridge: passed'
            echo '- Approved migration 061 compatibility: passed without weakening the runtime-rebrand boundary'
            echo '- Migration 054 lifecycle, .NET API, frontend production bundle, and web container: passed'
            echo '- Azure, Entra, provider-secret, deployment, and business-record mutation: none'
          } >> "$GITHUB_STEP_SUMMARY"
