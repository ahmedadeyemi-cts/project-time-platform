name: ProjectPulse Deploy Dashboard Calendar Test

on:
  workflow_dispatch:
    inputs:
      release_commit:
        description: Exact verified dashboard calendar release commit
        required: true
        default: e6eac94ae774311beb70c46c60213eda32aeda24
        type: string
      confirmation:
        description: Enter DEPLOY-DASHBOARD-CALENDAR-TO-TEST
        required: true
        type: string

permissions:
  id-token: write
  contents: read

concurrency:
  group: projectpulse-deploy-dashboard-calendar-test
  cancel-in-progress: false

jobs:
  deploy-web:
    name: Guarded dashboard calendar web-only test deployment
    environment: test
    runs-on: ubuntu-latest
    env:
      EXPECTED_RELEASE_COMMIT: e6eac94ae774311beb70c46c60213eda32aeda24
    steps:
      - name: Check out deployment controls
        uses: actions/checkout@v5
        with:
          path: control
          fetch-depth: 0

      - name: Check out exact dashboard calendar source
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.release_commit }}
          path: release
          fetch-depth: 1

      - name: Guard workflow source and dashboard calendar commit
        id: release
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ '${{ github.ref }}' == 'refs/heads/main' ]] || {
            echo 'This workflow must be dispatched from main.' >&2
            exit 1
          }
          [[ '${{ inputs.confirmation }}' == 'DEPLOY-DASHBOARD-CALENDAR-TO-TEST' ]] || {
            echo 'The deployment confirmation text does not match.' >&2
            exit 1
          }
          [[ '${{ inputs.release_commit }}' =~ ^[0-9a-f]{40}$ ]] || {
            echo 'release_commit must be a complete 40-character SHA.' >&2
            exit 1
          }

          CONTROL_COMMIT="$(git -C control rev-parse HEAD)"
          TARGET_COMMIT="$(git -C release rev-parse HEAD)"
          [[ "$CONTROL_COMMIT" == '${{ github.sha }}' ]] || {
            echo 'The checked-out control commit does not match this workflow run.' >&2
            exit 1
          }
          [[ "$TARGET_COMMIT" == '${{ inputs.release_commit }}' ]] || {
            echo 'The checked-out release does not match the requested commit.' >&2
            exit 1
          }
          [[ "$TARGET_COMMIT" == "$EXPECTED_RELEASE_COMMIT" ]] || {
            echo 'Only the verified dashboard calendar release may be deployed.' >&2
            exit 1
          }
          git -C control merge-base --is-ancestor "$TARGET_COMMIT" HEAD || {
            echo 'The dashboard calendar release is not an ancestor of the deployment-control commit.' >&2
            exit 1
          }

          PORTAL='release/src/frontend/project-time-web/src/DashboardPersonalCalendarPortal.jsx'
          CONTEXT='release/src/frontend/project-time-web/src/PageContextGuide.jsx'
          MAIN='release/src/frontend/project-time-web/src/main.jsx'
          CSS='release/src/frontend/project-time-web/src/dashboard-personal-calendar.css'

          grep -Fq 'dashboard-personal-calendar-host' "$PORTAL"
          grep -Fq "resourceIds: [currentUserId]" "$PORTAL"
          grep -Fq "label.textContent = 'Current week approvals'" "$PORTAL"
          grep -Fq "targetRow.dataset.dashboardApprovalScope = 'current-week'" "$PORTAL"
          grep -Fq 'new MutationObserver(scheduleApprovalSynchronization)' "$PORTAL"
          grep -Fq "import DashboardPersonalCalendarPortal" "$MAIN"
          grep -Fq '<DashboardPersonalCalendarPortal />' "$MAIN"
          grep -Fq '.dashboard-personal-calendar-board' "$CSS"
          grep -Fq '<details>' "$CONTEXT"
          if grep -Eq '<details[[:space:]]+open([[:space:]=>]|$)' "$CONTEXT"; then
            echo 'Page Context Guide must be collapsed by default.' >&2
            exit 1
          fi

          echo "target_commit=$TARGET_COMMIT" >> "$GITHUB_OUTPUT"

      - uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Capture current web image
        id: before
        shell: bash
        run: |
          set -Eeuo pipefail
          RAW_OLD_WEB_IMAGE="$(az containerapp show -g '${{ vars.AZURE_RESOURCE_GROUP }}' -n '${{ vars.AZURE_WEB_APP }}' --query 'properties.template.containers[0].image' -o tsv)"
          WEB_REVISION_MODE="$(az containerapp show -g '${{ vars.AZURE_RESOURCE_GROUP }}' -n '${{ vars.AZURE_WEB_APP }}' --query 'properties.configuration.activeRevisionsMode' -o tsv)"
          [[ -n "$RAW_OLD_WEB_IMAGE" ]] || {
            echo 'Could not capture the active web image.' >&2
            exit 1
          }
          [[ "${WEB_REVISION_MODE,,}" == 'single' ]] || {
            echo 'The web Container App must use single-revision mode.' >&2
            exit 1
          }

          resolve_acr_image_digest() {
            local image="$1"
            local registry='${{ vars.AZURE_ACR_NAME }}.azurecr.io/'
            local relative repository digest
            case "$image" in
              "$registry"*@sha256:*) printf '%s\n' "$image"; return 0 ;;
              "$registry"*:*) ;;
              *) echo "Rollback image is outside the approved ACR: $image" >&2; return 1 ;;
            esac
            relative="${image#"$registry"}"
            repository="${relative%:*}"
            digest="$(az acr repository show --name '${{ vars.AZURE_ACR_NAME }}' --image "$relative" --query digest -o tsv)"
            [[ "$digest" == sha256:* ]] || {
              echo "Could not resolve rollback digest for $image." >&2
              return 1
            }
            printf '%s%s@%s\n' "$registry" "$repository" "$digest"
          }

          OLD_WEB_IMAGE="$(resolve_acr_image_digest "$RAW_OLD_WEB_IMAGE")"
          echo "old_web_image=$OLD_WEB_IMAGE" >> "$GITHUB_OUTPUT"
          echo "CURRENT_WEB_IMAGE_IMMUTABLE=$OLD_WEB_IMAGE"

      - name: Build exact dashboard calendar web image
        id: build
        shell: bash
        working-directory: release
        run: |
          set -Eeuo pipefail
          TARGET_COMMIT='${{ steps.release.outputs.target_commit }}'
          WEB_REPOSITORY='project-health-dashboard-web'
          RELEASE_TAG="dashboard-calendar-$TARGET_COMMIT"
          WEB_DIGEST="$(
            ../control/scripts/build-pr55-acr-image.sh \
              '${{ vars.AZURE_ACR_NAME }}' \
              "$WEB_REPOSITORY:$RELEASE_TAG" \
              deployment/containers/web/Dockerfile \
              .
          )"
          WEB_IMAGE='${{ vars.AZURE_ACR_NAME }}.azurecr.io/'"$WEB_REPOSITORY@$WEB_DIGEST"
          echo "web_image=$WEB_IMAGE" >> "$GITHUB_OUTPUT"
          echo "IMMUTABLE_WEB_IMAGE=$WEB_IMAGE"

      - name: Deploy dashboard calendar web image only
        id: deploy_web
        shell: bash
        run: |
          set -Eeuo pipefail
          echo 'started=true' >> "$GITHUB_OUTPUT"
          az containerapp update \
            -g '${{ vars.AZURE_RESOURCE_GROUP }}' \
            -n '${{ vars.AZURE_WEB_APP }}' \
            --image '${{ steps.build.outputs.web_image }}' \
            --revision-suffix "dashcal-${GITHUB_RUN_ID}" \
            --output none

      - name: Validate served dashboard calendar assets and web image
        shell: bash
        run: |
          set -Eeuo pipefail
          BASE_URL='${{ vars.PUBLIC_URL }}'
          BASE_URL="${BASE_URL%/}"
          READY='false'
          LAST_STATUS='not-requested'
          LAST_JS='not-resolved'
          LAST_CSS='not-resolved'

          absolute_asset_url() {
            local asset_path="$1"
            case "$asset_path" in
              http://*|https://*) printf '%s\n' "$asset_path" ;;
              /*) printf '%s%s\n' "$BASE_URL" "$asset_path" ;;
              *) printf '%s/%s\n' "$BASE_URL" "$asset_path" ;;
            esac
          }

          for attempt in $(seq 1 30); do
            CACHE_BUSTER="dashcal-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}-${attempt}"
            STATUS="$(curl --silent --show-error --location --header 'Cache-Control: no-cache' --header 'Pragma: no-cache' --output /tmp/dashcal-index.html --write-out '%{http_code}' "$BASE_URL/?release_check=$CACHE_BUSTER" || true)"
            LAST_STATUS="$STATUS"
            LAST_JS='not-resolved'
            LAST_CSS='not-resolved'

            if [[ "$STATUS" == '200' ]]; then
              JS_PATH="$(sed -nE 's/.*src="([^"]+\.js)".*/\1/p' /tmp/dashcal-index.html | head -1)"
              mapfile -t CSS_PATHS < <(sed -nE 's/.*href="([^"]+\.css)".*/\1/p' /tmp/dashcal-index.html)
              if [[ -n "$JS_PATH" && ${#CSS_PATHS[@]} -gt 0 ]]; then
                LAST_JS="$JS_PATH"
                LAST_CSS="$(IFS='|'; echo "${CSS_PATHS[*]}")"
                JS_URL="$(absolute_asset_url "$JS_PATH")"
                JS_STATUS="$(curl --silent --show-error --location --header 'Cache-Control: no-cache' --header 'Pragma: no-cache' --output /tmp/dashcal-app.js --write-out '%{http_code}' "$JS_URL?release_check=$CACHE_BUSTER" || true)"

                : > /tmp/dashcal-all.css
                CSS_OK='true'
                for CSS_PATH in "${CSS_PATHS[@]}"; do
                  CSS_URL="$(absolute_asset_url "$CSS_PATH")"
                  CSS_STATUS="$(curl --silent --show-error --location --header 'Cache-Control: no-cache' --header 'Pragma: no-cache' --output /tmp/dashcal-one.css --write-out '%{http_code}' "$CSS_URL?release_check=$CACHE_BUSTER" || true)"
                  if [[ "$CSS_STATUS" != '200' ]]; then CSS_OK='false'; fi
                  cat /tmp/dashcal-one.css >> /tmp/dashcal-all.css
                done

                LAST_STATUS="html=$STATUS,js=$JS_STATUS,css=$CSS_OK"
                tr -d '[:space:]' < /tmp/dashcal-all.css > /tmp/dashcal-all.compact.css
                if [[ "$JS_STATUS" == '200' && "$CSS_OK" == 'true' ]] \
                  && grep -Fq 'dashboard-personal-calendar-host' /tmp/dashcal-app.js \
                  && grep -Fq 'Current week approvals' /tmp/dashcal-app.js \
                  && grep -Fq '/api/calendar/schedule' /tmp/dashcal-app.js \
                  && grep -Fq 'What this page does and what backend process supports it' /tmp/dashcal-app.js \
                  && grep -Fq 'dashboard-personal-calendar-board' /tmp/dashcal-all.compact.css \
                  && grep -Fq 'dashboard-personal-calendar-days' /tmp/dashcal-all.compact.css; then
                  READY='true'
                  echo "DASHBOARD_CALENDAR_ASSETS_READY attempt=$attempt js=$JS_PATH css=$LAST_CSS"
                  break
                fi
              fi
            fi
            (( attempt < 30 )) && sleep 10
          done

          [[ "$READY" == 'true' ]] || {
            echo "Dashboard calendar assets did not become ready: HTTP=$LAST_STATUS JS=$LAST_JS CSS=$LAST_CSS" >&2
            exit 1
          }

          ACTIVE_WEB_IMAGE="$(az containerapp show -g '${{ vars.AZURE_RESOURCE_GROUP }}' -n '${{ vars.AZURE_WEB_APP }}' --query 'properties.template.containers[0].image' -o tsv)"
          [[ "$ACTIVE_WEB_IMAGE" == '${{ steps.build.outputs.web_image }}' ]] || {
            echo "Active web image mismatch: expected=${{ steps.build.outputs.web_image }} actual=$ACTIVE_WEB_IMAGE" >&2
            exit 1
          }
          echo "DEPLOYED_DASHBOARD_CALENDAR_COMMIT=${{ steps.release.outputs.target_commit }}"
          echo "DEPLOYED_WEB_IMAGE=$ACTIVE_WEB_IMAGE"

      - name: Write dashboard calendar deployment evidence
        if: success()
        shell: bash
        run: |
          set -Eeuo pipefail
          mkdir -p evidence
          cat > evidence/dashboard-calendar-test-deployment.json <<JSON
          {
            "environment": "test",
            "releaseCommit": "${{ steps.release.outputs.target_commit }}",
            "workflowCommit": "${{ github.sha }}",
            "runId": "${{ github.run_id }}",
            "webImage": "${{ steps.build.outputs.web_image }}",
            "previousWebImage": "${{ steps.before.outputs.old_web_image }}",
            "apiDeployment": "unchanged",
            "migrations": "unchanged",
            "servedAssetValidation": "passed"
          }
          JSON

      - name: Upload dashboard calendar deployment evidence
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: dashboard-calendar-test-deployment-${{ github.run_id }}
          path: evidence/dashboard-calendar-test-deployment.json
          retention-days: 30

      - name: Roll back web image on failure
        if: ${{ failure() && steps.deploy_web.outputs.started == 'true' }}
        shell: bash
        run: |
          set +e
          echo 'Dashboard calendar deployment failed. Restoring the captured web image.' >&2
          az containerapp update \
            -g '${{ vars.AZURE_RESOURCE_GROUP }}' \
            -n '${{ vars.AZURE_WEB_APP }}' \
            --image '${{ steps.before.outputs.old_web_image }}' \
            --revision-suffix "dashcalrb-${GITHUB_RUN_ID}" \
            --output none
