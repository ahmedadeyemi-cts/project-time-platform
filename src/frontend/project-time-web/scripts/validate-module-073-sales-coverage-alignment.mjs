import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(process.cwd(),'..','..','..');const files={backend:'src/backend/ProjectTime.Api/Modules/SalesCoverageAlignmentModule.cs',frontend:'src/frontend/project-time-web/src/SalesCoverageAlignmentCenter.jsx',stylesheet:'src/frontend/project-time-web/src/sales-coverage-alignment-center.css',readme:'docs/modules/module-073-sales-coverage-alignment/README.md',api:'docs/modules/module-073-sales-coverage-alignment/API-CONTRACT.md',authorization:'docs/modules/module-073-sales-coverage-alignment/AUTHORIZATION-MATRIX.md',matrix:'docs/modules/module-073-sales-coverage-alignment/CAPABILITY-MATRIX.md',data:'docs/modules/module-073-sales-coverage-alignment/DATA-DESIGN.md',overlap:'docs/modules/module-073-sales-coverage-alignment/OVERLAP-AND-INTEGRATION.md',program:'src/backend/ProjectTime.Api/Program.cs',app:'src/frontend/project-time-web/src/App.jsx',package:'src/frontend/project-time-web/package.json',docker:'deployment/containers/web/Dockerfile'};
const exists=file=>fs.existsSync(path.join(root,file));const read=file=>fs.readFileSync(path.join(root,file),'utf8');const checks=[];function check(name,condition,evidence){checks.push(Boolean(condition));console.log(`MODULE_073_${name}=${condition?'PASSED':'FAILED'} — ${evidence}`)}for(const[name,file]of Object.entries(files))check(`${name.toUpperCase()}_EXISTS`,exists(file),file);
const backend=read(files.backend),frontend=read(files.frontend),css=read(files.stylesheet),docs=[files.readme,files.api,files.authorization,files.matrix,files.data,files.overlap].map(read).join('\n'),program=read(files.program),app=read(files.app),packageJson=JSON.parse(read(files.package)),docker=read(files.docker);const count=(value,marker)=>value.split(marker).length-1;
check('MAP_METHOD',backend.includes('MapSalesCoverageAlignmentEndpoints'),'isolated endpoint map');check('ENDPOINTS',['/capabilities','/source-signals','/identity-options','/validate'].every(value=>backend.includes(`/api/sales-coverage${value}`)),'complete source/draft contract');check('EVERYONE_VIEWS',backend.includes('canView = true')&&docs.includes('Every authenticated'),'view contract');check('ADMIN',backend.includes('"ADMINISTRATOR"')&&backend.includes('"SUPER_ADMINISTRATOR"'),'admin editors');check('SOLUTION_ARCHITECT',backend.includes('"SOLUTION_ARCHITECT"'),'SA editor');check('PTC',backend.includes('"PROJECT_TEAM_COORDINATOR"'),'PTC editor');check('ACTUAL_SESSION',backend.includes('ProjectPulseActualUserId')&&docs.includes('actual session'),'View-As safe');
check('IDENTITY_OPTIONS',backend.includes('identity_options_loaded')&&backend.includes('account_executive')&&backend.includes('resale_operations')&&backend.includes('solution_architect'),'role-aware identities');check('STABLE_DROPDOWNS',frontend.includes('<select value={row.accountExecutiveUserId}')&&frontend.includes('primaryResaleOperationsUserId')&&frontend.includes('solutionArchitectUserId'),'identity dropdowns');check('PRIMARY_BACKUP',backend.includes('primary_backup_must_differ'),'primary/backup rule');check('EFFECTIVE_DATES',backend.includes('effective_end_precedes_start')&&frontend.includes('type="date"'),'effective dating');check('TERRITORY_TEAM',backend.includes('["territory"]')&&backend.includes('["team"]'),'required mapping');check('SOURCE_SIGNALS',backend.includes('project_intake_requests')&&backend.includes('FROM projects'),'existing relationships preserved');check('DRAFT_ONLY',backend.includes('persistencePerformed = false')&&frontend.includes('data-persistence="unsaved-draft"'),'no save claim');check('ROW_LIMIT',backend.includes('rows.Count > 1000'),'bounded validation');check('CSV_EXPORT',frontend.includes('sales-coverage-alignment-draft.csv'),'handoff export');
check('NO_MUTATING_SQL',!/\b(?:INSERT|UPDATE|DELETE|ALTER|DROP|CREATE|TRUNCATE)\s+(?:INTO|TABLE|FROM|VIEW|INDEX|SCHEMA)\b/i.test(backend),'SELECT only');check('NO_DATABASE_ARTIFACT',!fs.existsSync(path.join(root,'database/migrations/073-sales-coverage-alignment.sql')),'no migration');check('US_SIGNAL_LOGO',frontend.includes('usSignalLogoDataUrl')&&frontend.includes('alt="US Signal"'),'brand logo');check('US_SIGNAL_BRAND',css.includes('--sales-blue')&&css.includes('--sales-cyan')&&css.includes('--sales-green'),'brand tokens');check('SCOPED_STYLES',!/(^|\n)\s*(?:body|html|\.panel|\.app-shell|\.sidebar)\s*\{/m.test(css),'styles scoped');check('PROGRAM_REGISTRATION',count(program,'app.MapSalesCoverageAlignmentEndpoints();')===1,'backend registered once');check('APP_IMPORT',count(app,"import SalesCoverageAlignmentCenter from './SalesCoverageAlignmentCenter.jsx';")===1,'frontend imported once');check('APP_MOUNT',count(app,'<SalesCoverageAlignmentCenter authSession={authSession} />')===1,'frontend mounted once');check('BUILD_GUARD',packageJson.scripts?.build?.includes('validate:module073')&&packageJson.scripts?.['validate:module073']?.includes('validate-module-073-sales-coverage-alignment.mjs'),'production build guard');check('CONTAINER_CONTEXT',docker.includes(files.backend)&&docker.includes('docs/modules/module-073-sales-coverage-alignment/'),'container validator context');check('REQUIREMENT',docs.includes('SAL-002'),'tracker requirement');check('OVERLAP_GATE',['Module 002','Module 064','Module 068'].every(value=>docs.includes(value))&&docs.includes('BLOCKED'),'concurrency gate');check('NO_EXTERNAL_MUTATION',docs.includes('Azure changed: no')&&docs.includes('Database changed: no')&&docs.includes('Entra changed: no'),'external state unchanged');
console.log(`\nMODULE_073_VALIDATION_CHECKS=${checks.length}`);console.log('MODULE_073_IMPLEMENTATION=FULL_SOURCE_VALIDATED_UNSAVED_DRAFT');console.log('MODULE_073_EDIT_ROLES=ADMINISTRATOR_SUPER_ADMINISTRATOR_SOLUTION_ARCHITECT_PROJECT_TEAM_COORDINATOR');console.log('MODULE_073_PERSISTENCE=LOCKED_PENDING_DATABASE_AUTHORIZATION');if(checks.some(value=>!value)){console.error('MODULE_073_CONTRACT=FAILED');process.exit(1)}console.log('MODULE_073_CONTRACT=PASSED');
