# Azure Migration Checkpoints

This directory records partial but important deployment checkpoints that occur between canonical phases.

Current entries:

- `AZ-05B4-PARTIAL-SERVER-CREATED.md` — PostgreSQL Flexible Server created; application database and post-create validation pending AZ-05B5 continuation.

Checkpoint files contain no passwords, connection strings, SAS tokens, or private keys.
