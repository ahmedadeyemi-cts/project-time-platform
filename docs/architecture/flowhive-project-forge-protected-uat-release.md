# FlowHive and Project Forge Protected UAT Release Evidence

This file is the permanent release-evidence marker for PR #737.

The candidate release unifies FlowHive and Project Forge around project-scoped, private, document-grounded AI planning. It resolves the existing active Work Register SOW by project ID, uses the current GSD and other authorized project documents as supplemental evidence, queues private processing, creates source citations, and expands every cited work package into Plan, Design, Implement, Validate, and Release tasks.

The release preserves source-backed effort and duration, calculates dependencies, dates, milestones, schedule variance, and critical path, and does not silently compress estimates. FlowHive saves a mutable working copy only; immutable version creation and reviewed baselining remain explicit review actions. Project Forge preserves its review and adoption governance.

Protected Test migration order is 094, 095, and 096 after the existing prerequisite migrations. The release is authorized for Protected UAT/Test only. Production mutation is not authorized.

This externally published marker triggers the repository's protected PR validation matrix after the automated source-convergence publisher has completed.
