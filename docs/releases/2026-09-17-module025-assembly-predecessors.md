# Module 025 post-generation dependency assembly

Protected Test [run 35244619086](https://github.com/ahmedadeyemi-cts/project-time-platform/actions/runs/35244619086/job/105281498382) failed on 2026-09-17 at 16:31:24 UTC on commit `5205140a5b12cbb123cc3dff5ef4c8200e22e98c`. Build, migrations, API/web deployment, installed identity and My Role browser acceptance passed. The first failing step was the scoped Module 025 lifecycle. Image and API-configuration rollback steps were skipped. The candidate was left installed in Test; Production was not changed and FlowHive generation was excluded.

Artifact `10507802787` (`systemwide-enterprise-reliability-test-evidence-35244619086-1`) has ZIP SHA256 `c2a971f405d64cb50f5589a940ea74eb53b1fc073dd497e25b013cf007699ee3`. Its generation evidence records all five phases completed and checkpointed with Claude `claude-opus-4-8`, one successful attempt per phase. Combined provider time was 323.227 seconds. Each response ended normally and passed the phase contract. The terminal diagnostic was `jsonexception` at `execute_generation`, after all five completed phases. Confirmation and retained SOW/GSD downloads were not reached. OpenAI was not called by this full-generation route.

The artifact contains progress metadata, not the saved phase payloads or the API exception stack. It therefore does not prove the exact live offending field. Offline reproduction identified a concrete post-phase defect with the same exception class and stage: `AssembleModule025PhasePlans` used `FirstOrDefault()` on value-tuple arrays, then assigned the default tuple to a nullable tuple. An empty candidate array consequently appeared non-null, with a null canonical WBS. Assembly inserted that null into the predecessor string list; final validation threw `JsonException: Module 025 detailed-plan list fields must contain strings or text-bearing objects.` This was reproduced before the fix through the real engine using five independently valid, serialized checkpoints and no provider calls.

The repair checks candidate presence explicitly. Missing, future-only and self references follow the existing policy: remove the unresolved edge and retain an explicit PM/Engineering review question. Valid current-phase fallback and prior-phase references retain their intended resolution order. Canonical WBS normalization remains deterministic. No task description, steps, responsibilities, deliverables, effort, acceptance criteria or other authored task content is reduced or replaced.

The engine also records sanitized `assembly_started` and `assembly_completed` progress after the five phases. These metadata-only events distinguish a finalization failure from provider failure. Assembly completion is not document publication, confirmation, download or acceptance success. The journal continues to retain only `phase_completed` results as reusable checkpoints, with its existing source, revision, archive and authorization guards.

Regression coverage replays all five saved phases with missing, future, self and valid dependencies; it asserts no AI calls, no null predecessors, explicit unresolved questions, preserved task detail and successful complete-document validation. Existing duplicate phase-local numbering and large detailed-document tests remain required. A PostgreSQL test reloads all five checkpoints through a fresh journal, assembles without inference, and verifies that assembly progress does not discard checkpoints or publish draft snapshots/versions. CI supplies the disposable PostgreSQL service.

This change does not alter providers, routing, AI budgets, privacy controls, schema requirements, Celar runtime, database schema, Test/Production deployment workflows or deployment authority. Its exact file set is registered in the existing read-only CI branch checks. No live retry was invoked as part of the repair. Full normal-SA lifecycle acceptance remains required after installation; SELL document publication remains separately blocked by the unavailable write adapter and verified document receipts.
