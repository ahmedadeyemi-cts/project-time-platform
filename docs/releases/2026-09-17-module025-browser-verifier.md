# Module 025 installed-browser verification repair

Protected Test [run 35249650844](https://github.com/ahmedadeyemi-cts/project-time-platform/actions/runs/35249650844/job/105298554348) installed application `3b7185292c013cd032b87ce6764ea238813e9e7a` on 2026-09-17. API/web deployment, installed identity and My Role passed; rollback steps were skipped. The first failing step was scoped Module 025 acceptance, with `module025_browser_timeout`.

Artifact `10511401132`, ZIP SHA256 `0f947129dc9629bf81a18a3a9b59a3f15905a27cb346fdf79f6f1a5c585a2a57`, now proves successful generation and assembly. Claude completed all five phases in one attempt each. The saved phase detail counts were Plan 93, Design 57, Implement 86, Validate 75 and Release 89. The verifier reviewed and saved generated output without changing its source, confirmed the SOW, and read back immutable version 1 (`0b26021b-e2c3-4cf5-b0a0-1aa94f4a9f46`, source revision 5). It recorded both retained document hashes. The full browser lifecycle did not pass. SELL remained blocked with `SELL_DOCUMENT_WRITE_ADAPTER_REQUIRED`.

The previous browser verifier stored browser progress only after the entire journey succeeded, and collapsed every Playwright timeout into the same diagnostic. This artifact therefore cannot identify its exact first failing browser action. No document files were included in this artifact, so it does not establish successful browser downloads or that the later reload defect was this run's first failure.

Offline reproduction runs the actual Python `browser_lifecycle` function against the real React workspace with a synthetic loopback HTTP server. Before the correction it downloaded both documents, reopened and saved the record, then deterministically timed out after reload: the workspace reloads with no selected record, but the verifier waited for an editor without selecting the record again. The corrected verifier reselects the same immutable SOW identity before checking saved content. No application UI, API, model or generation behavior changes.

The regression exercises the full actual browser sequence, repeated hash-checked SOW/GSD downloads, exactly one reopen and one save, persistence after reload, and zero generation requests. It also injects a denied document download and verifies an exact `download_sow` timeout stage and sanitized HTTP 401 evidence. The existing browser test covered visible actions, but did not execute this Python sequence; CI now does both.

The verifier now records completed steps and each download as it occurs, prints fixed step labels, and reports the exact timed-out step. Failed Module 025 responses expose only a closed endpoint category and HTTP status, never response content, URLs, query strings or session headers. A read-only browser preflight checks navigation, normal-SA visibility, record selection and document action visibility before the first AI request. It aborts Module 025 mutation requests during preflight. Unit coverage verifies a failed preflight results in zero generation calls and normal synthetic-record cleanup.

The next live step is the existing installed-release verification workflow from main, with `deployment_run_id=35249650844` and `acceptance_scope=sow_role`. It verifies the same deployed application and runs My Role without building images, applying migrations or deploying. Its full SOW acceptance still creates a new synthetic record and generates scope; it must not be described as inference-free. The earlier verifier attempted to archive its synthetic record in cleanup, so this repair does not promise resumable edits against that archived record. A browser preflight failure now stops before inference.

All deployment workflows, native environment protection, release authority, runtime source, provider configuration, model budgets, production state and FlowHive generation remain unchanged. Live browser acceptance is pending; this repair does not turn CI or generation success into a claim of complete Module 025 or SELL acceptance.

Local validation passed the real browser regression, 31 installed-acceptance tests, 32 current resolver tests, 83 admission tests, 19 release-workflow tests and 3 complete-acceptance tests. The obsolete `flowhive-psa-installed-deployment.test.py` standalone fixture still fails its positive case because it supplies `test/repo` to a resolver that requires this repository's real identity; both files are unchanged from the base. The maintained 32-case resolver suite exercises the real repository identity, exact installation/rollback bounds and newer-verifier support. This repair does not weaken that identity guard.
