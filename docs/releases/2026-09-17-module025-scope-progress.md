# Module 025 scope normalization and review feedback

The exact reported Service Overview, `Upgrading Cisco Call manager from version 14.0 to 15.0`, failed the external adapter before inference: both the technology alias and the verb inflection were absent. A regression fails on the previous release and verifies that the correction produces the same closed technical capsule as the canonical CUCM upgrade input, including both versions. No customer identity or raw source is sent. Exclusions, privacy policy, output validation, attempt budgets and provider order remain unchanged.

Generated effort rationale now preserves each work package’s hours, provider-authored explanation, included work, prerequisites and unresolved inputs. Phase totals sum the same package estimates shown in the rationale. The previous generic Celar-branded sentence is removed. Historical records are preserved; a failed regeneration does not rewrite them.

The workspace identifies the current phase and provider, completed phases and elapsed time. Terminal failures explain that the visible scope and hours are the previous saved result, and include the existing safe diagnostic and provider result codes. Existing generated records with legacy metadata no longer show “Not generated” as their confidence. Generation locks editing of the selected draft, and completion does not switch the user back from a different selected record.

Validation uses synthetic provider responses only. No live inference or deployment is started by these tests. The earlier passing lifecycle run demonstrated a different synthetic record, not the user's historical SOW. The current private-provider failure requires its generation diagnostics; the screenshot alone does not identify that provider's root cause.

Deployment authority, environment approvals, Production, database migrations, FlowHive generation, and the SELL write adapter are unchanged.
