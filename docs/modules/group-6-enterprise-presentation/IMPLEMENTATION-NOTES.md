# Group 6 implementation notes

The shared presentation system is installed during the existing generated-App sequence. It adds one route-aware enterprise header after `PageContextGuide`, while route-specific CSS normalizes the existing module panels underneath it.

The canonical `App.jsx`, `main.jsx`, module registry, More menu, permissions, APIs, and database schema are not directly changed by this source package.
