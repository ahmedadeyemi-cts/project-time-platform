# Caddy Reverse Proxy

This folder will contain reverse proxy configuration for Caddy if selected.

Planned contents:

- Caddyfile templates
- HTTPS configuration
- Application routing
- Cloudflare DNS/proxy notes
- Validation steps
