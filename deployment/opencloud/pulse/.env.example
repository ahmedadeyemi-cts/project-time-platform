# Non-secret operator configuration. Do not run from this example unchanged.
# Image references must use registry/name@sha256:<verified manifest digest>.
TARGET_PLATFORM=linux/amd64
HTTPS_BIND_IP=0.0.0.0
PULSE_DB_NAME=ProjectPulse
PULSE_DB_USER=ptp_app
PULSE_LOCAL_DIR=/srv/pulse-dev/config
PULSE_API_IMAGE=
PULSE_WEB_IMAGE=
POSTGRES_IMAGE=
CADDY_IMAGE=
