# Secure host-local runtime settings, not for GitHub, images or OneDrive bundles.
# Provision separately at PULSE_LOCAL_DIR/pulse-runtime.env (root:API-group 0640).
# Do NOT copy legacy ConnectionStrings__* values or PTP_DB_PASSWORD here.
# api-entrypoint.sh obtains the runtime database password from its secret file.
# Supply the approved SSO settings and exact callback path after registration.
# PROJECTPULSE_SSO_TENANT_ID=
# PROJECTPULSE_SSO_CLIENT_ID=
# PROJECTPULSE_SSO_REDIRECT_URI=
# PROJECTPULSE_SSO_CLIENT_SECRET=
# Preserve and securely migrate the existing integration/key-ring configuration.
# PROJECTPULSE_INTEGRATION_SECRET_ENCRYPTION_KEY=
# PROJECTPULSE_MICROSOFT_INTEGRATION_SECRET_KEY=
# PROJECTPULSE_AI_SECRET_ENCRYPTION_KEY_RING=
# AI release/controller configuration must be generated by the approved pipeline.
# DO NOT drop the existing release phase to evade its checks.
# The current Oracle-only external-HTTPS policy does not yet authorize the new
# Celar hostname. See readiness.json; do not change DNS or weaken TLS validation.
# This file intentionally contains no working credentials or fake defaults.
